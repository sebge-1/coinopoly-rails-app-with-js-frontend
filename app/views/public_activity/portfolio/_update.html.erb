<% if activity.trackable %>
  has just updated their <%= link_to activity.trackable.name, user_portfolio_path(activity.trackable.user, activity.trackable) %> portfolio.
<% else %>
  has updated a portfolio which has since been removed.
<% end %>
