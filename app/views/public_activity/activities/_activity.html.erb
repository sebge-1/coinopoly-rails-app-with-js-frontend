<% if displayable?(activity) && activity.owner %>
  [<%= activity.created_at.localtime.strftime('%H:%M:%S %-d %B %Y')
  %>] <br>
  <%= link_to activity.owner.name, activity.owner %><%= render_activity activity %>
  <hr>
<% end %>
