<%= @portfolio.name %>

<!-- <% @portfolio.positions.each do |position| %>
  <%= position.coin.name %>
  <%= position.quantity %>
<% end %> -->

<%= form_for [@portfolio, @position] do |f| %>
  <label>Coin:</label>
  <%= f.collection_select(:coin_id, @coins, :id, :name) %>
  <label>Quantity:</label>
  <%= f.number_field :quantity %>
  <%= f.submit 'Add a balance to your portfolio' %>
<% end %>
